server:
  port: 8081
spring:
  application:
    name: microservice-config
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/cvstleee/TINGESO_MICROSERVICE
          search-paths: "config-data"
